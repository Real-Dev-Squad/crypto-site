import React from 'react';
import { walletItemProps } from './walletDetails.type';

const WalletItem: React.FC<walletItemProps> = ({ currency, value }) => {
  return <></>
};

export default WalletItem;
